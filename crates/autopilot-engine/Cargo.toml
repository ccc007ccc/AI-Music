[package]
name = "autopilot-engine"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
audio-engine = { path = "../audio-engine" }
composition-engine = { path = "../composition-engine" }
music-core = { path = "../music-core" }
schemars = "1.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3"
thiserror = "2"
wait-timeout = "0.2"
